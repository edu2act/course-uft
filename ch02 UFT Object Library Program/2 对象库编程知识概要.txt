1 添加对象（登录链接、用户名框、密码框等等）
2 使用对象，完成对象库编程
	校验
	参数化
	打印测试报告

1 添加对象库
	1）正常添加
	2）切换鼠标点击（Ctrl键）
	3）切换程序（Ctrl+Alt）
	4）spy来添加对象（支持鼠标点击 用ctrl 键；支持鼠标点击和键盘输入，用Ctrl+Alt键）
2 使用对象库
	1）步骤生成器
	2）Complete Word
	3)拖动
3 编辑代码：
	1）修改代码
	2）修改对象库（将对象库的标识改的简短一些）
	3）获取对象关键属性及属性值，与TO做对比，如果一致，则表明该用例执行成功	，如果不一致，则执行失败（其中获取对象使用GetProperty）
	4）垂直选择（Alt+鼠标左键拖动）
	5）多条语句含有相同部分，使…d With
	6)换行

补充内容：
1 对象库管理：删除、复制、剪切、粘贴
2 对象库共享使用：保存、导出、关联；
	比较，合并
3 对象库内置对象：SystemUtil(Run,CloseProcessByName(或ByID))

4 GetROProperty,GetTOProperty,TetTOProperties

VBS 语法基础
1 变量定义和使用
	1）先声明，后赋值（分两行）
	2）没有数据类型的声明
	3）通常使用显示声明
	4）字母开头，不加.,长度小于255字符
2 数组
	1）定长数组   先声明，后赋值
	2）括号使用小括号

3 操作符   ^  Mod  = <>
4 条件语句
	IF  Then  Else End IF
	IF  Then ElseIF Then Else End IF
	Select Case 
		Case 变量值
		执行操作语句
	select End
5 循环
	For Next 
	Do    Loop while
7 常用的函数：字符类、日期类、转换类、其他函数








